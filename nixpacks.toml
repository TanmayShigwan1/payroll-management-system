[providers]
java = "17"

[phases.build]
cmd = "cd backend && ./mvnw clean package -DskipTests || mvn clean package -DskipTests"

[phases.start]
cmd = "cd backend && java -jar target/*.jar"

[variables]
MAVEN_OPTS = "-Xmx1024m"
